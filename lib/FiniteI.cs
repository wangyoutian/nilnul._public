using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace nilnul
{
	/// <summary>
	/// the type has finite instances. for example int. 
	/// while IComparer is inifinte. for it can have as many instances as possible.
	/// particle, though every instance looks the same except location, is infinite in that every partial is a placeholder for properties that can be attached to describe color, postion, etc.
	/// </summary>
	public partial interface FiniteI
	{
	}
}
